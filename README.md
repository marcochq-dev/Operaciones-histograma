#Estas operaciones se ejecutan en colab de google
#Para ejecutar los codigos de operaciones de procesamiento de imagenes, instalar las siguientes librerias
 
!pip install nvcc4jupyter

%load_ext nvcc4jupyter

!pip install opencv-python-headless

!pip install pycuda
